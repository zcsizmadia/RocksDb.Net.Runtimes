<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <NativeArch>linux-x64</NativeArch>
    <NativeLib>librocksdb.so</NativeLib>
  </PropertyGroup>

  <Import Project="../RocksDb.Net.Runtime.csproj.template" />

  <ItemGroup>
    <Content Include="$(NativePath)librocksdb.so" Pack="true" PackagePath="$(PackageNativePath)" />
    <Content Include="$(NativePath)libsnappy.1.so" Pack="true" PackagePath="$(PackageNativePath)" />
    <Content Include="$(NativePath)libz.1.so" Pack="true" PackagePath="$(PackageNativePath)" />
  </ItemGroup>
</Project>
